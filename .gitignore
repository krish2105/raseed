node_modules/
.pnpm-store/

# build output
.next/
out/
dist/
.turbo/

# expo — native dirs are generated by prebuild (CNG), never committed
.expo/
apps/mobile/ios/
apps/mobile/android/
# A prebuild run from the repo ROOT (rather than apps/mobile) drops these here, where the
# pattern above cannot reach them. It happened once, and the committed project carried a
# different bundle identifier than CLAUDE.md mandates.
/ios/
/android/
/app.json
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# env
.env
.env.*
!.env.example

# misc
*.log
npm-debug.log*
yarn-error.log*
.DS_Store
coverage/
*.tsbuildinfo
.vercel
.env*

# Copied from node_modules at build time; ~75MB has no business in git.
apps/web/public/duckdb/

# Playwright
apps/web/test-results/
apps/web/playwright-report/
